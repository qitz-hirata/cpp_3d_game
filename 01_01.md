# 第1章（続）キー入力によるCubeの移動とカメラの回転

## この節で学ぶこと

この節では、キーボードの入力を受け取って Cube を動かし、さらにカメラを自由に回転させる機能を実装します。

具体的には次の3つのステップで進めます。

1. `DxLibCommon` にキー入力ラッパーを追加し、左右矢印キーで Cube を X 軸方向に移動させる
2. `GameKey` 列挙型でキー入力 API を整理し、上下矢印キーで Z 軸移動を追加する
3. WASD キーでカメラを Y 軸・X 軸まわりに回転させる（球面座標）

---

## 1-4　DxLib のキー入力を DxLibCommon 経由で使う

### DxLib のキー入力 API

DxLib でキーが押されているかを調べるには `CheckHitKey(キーコード)` を使います。

```cpp
// これは DxLib の生の書き方（DxLibCommon.cpp の外では使えない）
if (CheckHitKey(KEY_INPUT_LEFT) != 0) {
    // 左矢印キーが押されている
}
```

戻り値は「押されていれば `1`、押されていなければ `0`」です。`!= 0` で `bool` に変換しています。

ただし `CheckHitKey` や `KEY_INPUT_LEFT` は `DxLib.h` で定義されているため、**DxLibCommon.cpp の外には出せません**（DxLib隔離パターン）。

```
main.cpp  ─→  DxLibCommon.h   DxLib.h を含めない
                  ↓
              DxLibCommon.cpp ─→ DxLib.h  ← ここだけで CheckHitKey を呼ぶ
```

---

### DxLibCommon にキー入力メソッドを追加する（最初の実装）

左右の矢印キーに対応した専用メソッドをまず追加しました。

#### ヘッダへの追加（DxLibCommon.h）

```cpp
// DxLibCommon.h
class DxLibCommon {
public:
    // ... 既存のメソッド ...

    bool IsLeftKeyDown();   // ← 追加
    bool IsRightKeyDown();  // ← 追加
};
```

#### 実装への追加（DxLibCommon.cpp）

```cpp
// DxLibCommon.cpp
bool DxLibCommon::IsLeftKeyDown()  { return CheckHitKey(KEY_INPUT_LEFT)  != 0; }
bool DxLibCommon::IsRightKeyDown() { return CheckHitKey(KEY_INPUT_RIGHT) != 0; }
```

DxLib の `CheckHitKey` を `DxLibCommon.cpp` の中だけで呼び出し、`main.cpp` 側には `bool`（真偽値）だけを返します。こうすることで DxLib の詳細を完全に隠蔽できます。

---

### main.cpp から Cube を動かす

キューブを動かすには `cube.transform.position` の値を毎フレーム変化させます。

```cpp
// main.cpp
static const float kMoveSpeed = 0.05f;  // 1フレームあたりの移動量

while (dxLibCommon->Running())
{
    // キー入力でCubeを左右に移動
    if (dxLibCommon->IsLeftKeyDown())  { cube.transform.position.x -= kMoveSpeed; }
    if (dxLibCommon->IsRightKeyDown()) { cube.transform.position.x += kMoveSpeed; }

    // ... 描画処理 ...
}
```

> **ポイント：毎フレーム位置を変える**
> ゲームループは1秒間に60回前後繰り返されます。キーが押されている間は毎フレーム `kMoveSpeed` だけ位置をずらすことで、なめらかな移動が実現できます。
> `kMoveSpeed` を大きくすると動きが速く、小さくすると遅くなります。

| キー | 変化 | 結果 |
|---|---|---|
| ← 左矢印 | `position.x -= 0.05f` | 左に移動 |
| → 右矢印 | `position.x += 0.05f` | 右に移動 |

---

## 1-5　キー入力 API の整理と上下移動の追加

### 問題：メソッドが増えすぎる

上下移動と WASD カメラ回転を追加するために、キーごとに専用メソッドを増やし続けると次のようになってしまいます。

```cpp
// NG例：メソッドが際限なく増える
bool IsLeftKeyDown();
bool IsRightKeyDown();
bool IsUpKeyDown();
bool IsDownKeyDown();
bool IsWKeyDown();
bool IsAKeyDown();
bool IsSKeyDown();
bool IsDKeyDown();
```

これでは追加のたびに `.h` と `.cpp` を両方書き換えなければならず、管理が大変です。

---

### 解決策：GameKey 列挙型の導入

```cpp
// DxLibCommon.h
enum class GameKey { Left, Right, Up, Down, W, A, S, D };
```

列挙型（`enum class`）とは「決まった選択肢の一覧」を定義する仕組みです。`GameKey::Left` のように `クラス名::値名` の形で使います。

> **`enum class` を使う理由**
> 単なる `enum` は他の数値型と混同されやすいバグを起こします。`enum class` は型が厳密に区別されるため安全です。例えば `GameKey::Left` は `int` の `0` とは別の型として扱われます。

---

### IsKeyDown に統合する

専用メソッドを廃止し、`IsKeyDown` 1つに統合します。

#### ヘッダの変更（DxLibCommon.h）

```cpp
// DxLibCommon.h
// （変更前）
bool IsLeftKeyDown();
bool IsRightKeyDown();

// （変更後）
bool IsKeyDown(GameKey key);
```

#### 実装（DxLibCommon.cpp）

```cpp
// DxLibCommon.cpp
bool DxLibCommon::IsKeyDown(GameKey key) {
    switch (key) {
        case GameKey::Left:  return CheckHitKey(KEY_INPUT_LEFT)  != 0;
        case GameKey::Right: return CheckHitKey(KEY_INPUT_RIGHT) != 0;
        case GameKey::Up:    return CheckHitKey(KEY_INPUT_UP)    != 0;
        case GameKey::Down:  return CheckHitKey(KEY_INPUT_DOWN)  != 0;
        case GameKey::W:     return CheckHitKey(KEY_INPUT_W)     != 0;
        case GameKey::A:     return CheckHitKey(KEY_INPUT_A)     != 0;
        case GameKey::S:     return CheckHitKey(KEY_INPUT_S)     != 0;
        case GameKey::D:     return CheckHitKey(KEY_INPUT_D)     != 0;
        default:             return false;
    }
}
```

`switch` 文で `GameKey` の種類ごとに DxLib のキーコードへ変換しています。`main.cpp` 側は `IsKeyDown(GameKey::Left)` のように呼ぶだけで、DxLib の詳細を一切知らなくてよくなります。

---

### 矢印キーで Cube を上下（Z 軸）にも移動させる

```cpp
// main.cpp
// 矢印キーでCubeを移動 (左右=X軸, 上下=Z軸)
if (dxLibCommon->IsKeyDown(GameKey::Left))  { cube.transform.position.x -= kMoveSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::Right)) { cube.transform.position.x += kMoveSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::Up))    { cube.transform.position.z += kMoveSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::Down))  { cube.transform.position.z -= kMoveSpeed; }
```

3D 空間での操作感として、上矢印は **Z 軸の奥（+Z 方向）**、下矢印は **Z 軸の手前（−Z 方向）** に移動します。

```
         +Z（奥）
          ↑
   −X ←  □  → +X
          ↓
         −Z（手前）
```

| キー | 変化 | 結果 |
|---|---|---|
| ← 左矢印 | `position.x -= 0.05f` | 左に移動 |
| → 右矢印 | `position.x += 0.05f` | 右に移動 |
| ↑ 上矢印 | `position.z += 0.05f` | 奥に移動 |
| ↓ 下矢印 | `position.z -= 0.05f` | 手前に移動 |

---

## 1-6　WASD でカメラを回転させる

### カメラの表し方：球面座標

これまでカメラの位置は固定の値 `{ 0.0f, 5.0f, -10.0f }` で指定していました。これを WASD で動かすために、**球面座標**という考え方を使います。

球面座標では、カメラの位置を次の3つの変数で表します。

| 変数 | 意味 | 初期値 |
|---|---|---|
| `kCameraDistance` | カメラと注視点の距離（固定） | `≈ 11.18` |
| `cameraYaw` | Y 軸まわりの角度（左右の向き） | `0.0f` |
| `cameraPitch` | X 軸まわりの角度（仰角） | `≈ 0.464 ラジアン` |

注視点（カメラが見る点）は常に原点 `{ 0, 0, 0 }` とします。

```
         Y
         │  ← カメラ
         │  ／  ↑ Distance（距離）
    Pitch│ ／
（仰角） │／
─────────┼──────── Z
       (0,0,0)
    ← Yaw（Y軸まわり） →
```

---

### 初期値の計算

元の固定カメラ位置 `{ 0, 5, -10 }` から初期の角度と距離を計算しています。

```cpp
static const float kCameraDistance = sqrtf(125.0f); // sqrt(5² + 10²) ≈ 11.18
float cameraYaw   = 0.0f;
float cameraPitch = atan2f(5.0f, 10.0f);             // ≈ 0.464 ラジアン
```

**`sqrtf(125.0f)` の意味**

元の位置 `{ 0, 5, -10 }` から原点 `{ 0, 0, 0 }` までの距離を計算しています。

```
距離 = sqrt(0² + 5² + (-10)²) = sqrt(0 + 25 + 100) = sqrt(125) ≈ 11.18
```

> **`sqrtf` とは**
> `sqrtf(x)` は「x の平方根（ルート）」を返す関数です（`<cmath>` で使用可能）。

**`atan2f(5.0f, 10.0f)` の意味**

水平距離 `10`（XZ 平面上での距離）に対して高さ `5` になるような仰角を求めています。

```
       カメラ
      ／│
     ／ │ 5（高さ）
    ／  │
───────────
   10（水平距離）
   ↓
仰角 = atan2f(5.0f, 10.0f) ≈ 0.464 ラジアン（≈ 26.6 度）
```

> **`atan2f` とは**
> `atan2f(y, x)` は「水平距離 x に対して高さ y になる角度（ラジアン）」を返す関数です。`<cmath>` で使用できます。

---

### 球面座標から XYZ 座標への変換

```cpp
Vector3 cameraPos = {
    kCameraDistance * sinf(cameraYaw) * cosf(cameraPitch),   // X 座標
    kCameraDistance * sinf(cameraPitch),                      // Y 座標
   -kCameraDistance * cosf(cameraYaw) * cosf(cameraPitch)    // Z 座標
};
```

> **`sinf` と `cosf` とは**
> - `sinf(角度)` ：その角度の「縦成分の割合（−1〜1）」を返す
> - `cosf(角度)` ：その角度の「横成分の割合（−1〜1）」を返す
>
> たとえば `Yaw = 0`、`Pitch = 0` のとき：
> - `X = Distance * sin(0) * cos(0) = Distance * 0 * 1 = 0`
> - `Y = Distance * sin(0) = 0`
> - `Z = -Distance * cos(0) * cos(0) = -Distance`
>
> つまりカメラは `{ 0, 0, -Distance }` の位置（真後ろ）に来ます。Pitch を増やすと Y が大きくなり（上に上がる）、Yaw を変えると X/Z が変化して左右に回転します。

**検算：初期値で元のカメラ位置になるか確認**

`Yaw = 0`、`Pitch = atan2f(5, 10)` を代入すると：

```
X = 11.18 * sin(0)        * cos(0.464) = 0          → 0   ✓
Y = 11.18 * sin(0.464)                 = 11.18*0.447 ≈ 5   ✓
Z = -11.18 * cos(0)       * cos(0.464) = -11.18*0.894 ≈ -10 ✓
```

元のカメラ位置 `{ 0, 5, -10 }` と一致します。

---

### WASD でカメラを回転させる

```cpp
// main.cpp
static const float kCameraSpeed = 0.02f;  // 1フレームあたりの回転量（ラジアン）

// WASDでカメラを回転 (A/D=Y軸, W/S=X軸)
if (dxLibCommon->IsKeyDown(GameKey::A)) { cameraYaw   -= kCameraSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::D)) { cameraYaw   += kCameraSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::W)) { cameraPitch += kCameraSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::S)) { cameraPitch -= kCameraSpeed; }
cameraPitch = std::clamp(cameraPitch, -1.5f, 1.5f);
```

| キー | 変化 | 見た目の動き |
|---|---|---|
| A | `cameraYaw -= 0.02f` | カメラが左に回転（右の景色が見える） |
| D | `cameraYaw += 0.02f` | カメラが右に回転（左の景色が見える） |
| W | `cameraPitch += 0.02f` | カメラが上に移動（見下ろす角度が大きくなる） |
| S | `cameraPitch -= 0.02f` | カメラが下に移動（見上げる角度が大きくなる） |

---

### std::clamp でカメラの仰角を制限する

```cpp
cameraPitch = std::clamp(cameraPitch, -1.5f, 1.5f);
```

`std::clamp(値, 最小値, 最大値)` は値を指定した範囲に収める関数です（`<algorithm>` のインクルードが必要）。

```cpp
std::clamp(2.0f,  -1.5f, 1.5f)  // → 1.5f  （最大値に丸められる）
std::clamp(-2.0f, -1.5f, 1.5f)  // → -1.5f （最小値に丸められる）
std::clamp(0.5f,  -1.5f, 1.5f)  // → 0.5f  （範囲内なのでそのまま）
```

`cameraPitch` を **±1.5 ラジアン（約 ±86 度）** に制限しているのは、カメラが真上・真下を向いた瞬間に起きる **ジンバルロック** という問題を防ぐためです。

> **ジンバルロックとは**
> カメラが真上（または真下）を向くと、Y 軸の回転（Yaw）と X 軸の回転（Pitch）が同じ軸に重なってしまい、カメラの向きの計算が破綻する現象です。π/2 ≈ 1.5708 ラジアンが「真上」なので、少し手前の 1.5 で止めることで回避しています。

---

### main.cpp のループ全体像

```cpp
#include <Windows.h>
#include <algorithm>   // std::clamp のために追加
#include <cassert>
#include <cmath>
#include <format>
#include "DxLibCommon.h"
#include "Vector3.h"
#include "Cube.h"

// ... (初期化処理は省略) ...

    static const float kMoveSpeed      = 0.05f;
    static const float kCameraSpeed    = 0.02f;
    static const float kCameraDistance = sqrtf(125.0f);  // 初期カメラ距離
    float cameraYaw   = 0.0f;
    float cameraPitch = atan2f(5.0f, 10.0f);              // 初期仰角

    while (dxLibCommon->Running())
    {
        // ① 矢印キーでCubeを移動 (左右=X軸, 上下=Z軸)
        if (dxLibCommon->IsKeyDown(GameKey::Left))  { cube.transform.position.x -= kMoveSpeed; }
        if (dxLibCommon->IsKeyDown(GameKey::Right)) { cube.transform.position.x += kMoveSpeed; }
        if (dxLibCommon->IsKeyDown(GameKey::Up))    { cube.transform.position.z += kMoveSpeed; }
        if (dxLibCommon->IsKeyDown(GameKey::Down))  { cube.transform.position.z -= kMoveSpeed; }

        // ② WASDでカメラを回転 (A/D=Y軸, W/S=X軸)
        if (dxLibCommon->IsKeyDown(GameKey::A)) { cameraYaw   -= kCameraSpeed; }
        if (dxLibCommon->IsKeyDown(GameKey::D)) { cameraYaw   += kCameraSpeed; }
        if (dxLibCommon->IsKeyDown(GameKey::W)) { cameraPitch += kCameraSpeed; }
        if (dxLibCommon->IsKeyDown(GameKey::S)) { cameraPitch -= kCameraSpeed; }
        cameraPitch = std::clamp(cameraPitch, -1.5f, 1.5f);

        // ③ 球面座標からカメラの XYZ 位置を計算
        Vector3 cameraPos = {
            kCameraDistance * sinf(cameraYaw) * cosf(cameraPitch),
            kCameraDistance * sinf(cameraPitch),
           -kCameraDistance * cosf(cameraYaw) * cosf(cameraPitch)
        };

        dxLibCommon->PreDraw();

        dxLibCommon->SetCamera(cameraPos, { 0.0f, 0.0f, 0.0f });  // ④ カメラを設定
        cube.Draw(dxLibCommon);

        dxLibCommon->PostDraw();
    }
```

**処理の流れ**

```
毎フレーム
 ①  矢印キー → Cube の position を更新
 ②  WASD    → cameraYaw / cameraPitch を更新
 ③  角度と距離 → cameraPos（XYZ）を計算
 ④  cameraPos を SetCamera に渡して描画
```

---

## まとめ

| 機能 | キー | 変更ファイル |
|---|---|---|
| Cube を左右に移動 | ← / → | DxLibCommon.h/cpp、main.cpp |
| Cube を前後に移動 | ↑ / ↓ | main.cpp |
| カメラを Y 軸まわりに回転 | A / D | main.cpp |
| カメラを X 軸まわりに回転 | W / S | main.cpp |

**この節の設計ポイント**

1. `DxLib.h` の `CheckHitKey` は `DxLibCommon.cpp` の中だけで呼び出す（DxLib 隔離パターン）
2. `GameKey` 列挙型をヘッダに定義することで、キーの名前を `main.cpp` 側でも安全に使える
3. カメラの位置は球面座標（距離・Yaw・Pitch）で管理し、毎フレーム XYZ 座標へ変換する
4. `cameraPitch` は `std::clamp` で ±1.5 ラジアンに制限してジンバルロックを回避する

---
