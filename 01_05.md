# 01_05 外部ライブラリの導入とCSVによる敵配置データの読み込み


## 1. fast-cpp-csv-parser の導入

今回使用するライブラリは **fast-cpp-csv-parser**（作者：Ben Strasser）です。`csv.h` 1枚からなるヘッダオンリーライブラリです。
https://github.com/ben-strasser/fast-cpp-csv-parser

### 1-1. ファイルの配置

GitHubから `csv.h` を取得し、`External` フォルダに配置します。

```
プロジェクト/
├─ External/
│   ├─ DxLib/              ← 既存の DxLib
│   └─ fast-cpp-csv-parser/
│       └─ csv.h           ← ここに置く ★
├─ Resources/
│   └─ enemy_data.csv
└─ main.cpp
```

`External` フォルダに置く理由は、**自分が書いたコードと外部から入手したコードを分けて管理する**ためです。このプロジェクトでは DxLib も同じ `External` 以下に置いています。

### 1-2. インクルードパスの追加（Visual Studio プロパティ画面）

`csv.h` を `#include "csv.h"` と書くだけで見つけてもらうには、Visual Studio にフォルダの場所を教える必要があります。これを **プロジェクトのプロパティ画面** から設定します。

**手順①　プロパティ画面を開く**

ソリューションエクスプローラーでプロジェクト名を **右クリック** → 「**プロパティ**」を選択します。

```
ソリューションエクスプローラー
└─ CPPDXGamePrograming  ← ここを右クリック
      └─ プロパティ(R)  ← クリック
```

**手順②　構成を「すべての構成」に切り替える**

プロパティ画面の左上にある「**構成**」ドロップダウンを **「すべての構成」** に変更します。
隣の「**プラットフォーム**」も **「すべてのプラットフォーム」** にします。

```
┌──────────────────────────────────────────────────┐
│ 構成(C):  [すべての構成          ▼]              │
│ プラット: [すべてのプラットフォーム ▼]            │
└──────────────────────────────────────────────────┘
```

> **ここが重要：** このプロジェクトは Debug/Release × Win32/x64 の **4構成** があります。
> 「すべての構成」にしてから設定することで、4構成まとめて変更できます。
> 個別に設定すると一部の構成でビルドエラーが残ります。

**手順③　追加のインクルードディレクトリを開く**

左のツリーから「**構成プロパティ**」→「**C/C++**」→「**全般**」を選択します。
右側のリストに「**追加のインクルードディレクトリ**」が表示されます。

```
構成プロパティ
└─ C/C++
    └─ 全般   ← ここを選択
         追加のインクルードディレクトリ: $(ProjectDir)External\DxLib;...
```

**手順④　パスを追加する**

「追加のインクルードディレクトリ」の欄をクリックすると右端に「**▼**」ボタンが現れます。
クリックして「**＜編集...＞**」を選ぶとダイアログが開きます。

ダイアログ上部の入力欄に以下を **1行追加** します。

```
$(ProjectDir)External\fast-cpp-csv-parser
```

```
┌─ 追加のインクルードディレクトリ ──────────────────┐
│  $(ProjectDir)External\DxLib          （既存）    │
│  $(ProjectDir)External\fast-cpp-csv-parser ← 追加 │
└───────────────────────────────────────────────────┘
```

「**OK**」→「**適用**」→「**OK**」でプロパティ画面を閉じます。

**`$(ProjectDir)` とは**

Visual Studio がビルド時に自動的にプロジェクトフォルダのパスに置き換える変数です。
フルパスを直書きしないことで、フォルダを移動しても設定が壊れません。

```
$(ProjectDir)External\fast-cpp-csv-parser
    ↓ ビルド時に展開
D:\DirectXGames\cpp_3d_game\External\fast-cpp-csv-parser
```

---

## 2. `#pragma warning` — 外部ライブラリの警告抑制

`csv.h` をインクルードするとビルド時に次のエラーが出ます。

```
error C4996: 'strncpy': This function or variable may be unsafe.
error C4996: 'fopen':   This function or variable may be unsafe.
```

これは MSVC（Visual Studio のコンパイラ）が `strncpy` や `fopen` を「安全でない関数」として警告しているためです。`csv.h` は外部ライブラリなので **私たちが中身を変えるのは NG** です。代わりに、インクルードする箇所だけ警告を一時的に無効にします。

```cpp
#pragma warning(push)              // 現在の警告設定を保存
#pragma warning(disable: 4996)     // C4996（安全でない関数警告）を無効化
#include "csv.h"                   // ← ここだけ警告が抑制される
#pragma warning(pop)               // 保存していた警告設定を復元
```

**`push` と `pop` のペアが重要**

```
コード全体の警告設定
     │
     ├─ #pragma warning(push)   ← 現在の設定を「スタック」に保存
     │
     │   C4996 を無効化した状態
     │   #include "csv.h"       ← この範囲だけ警告オフ
     │
     └─ #pragma warning(pop)    ← スタックから設定を復元
                                    → 以降は元の設定に戻る
```

`pop` を忘れると、`csv.h` より後のコード全体で警告が無効になり、自分が書いたコードの問題を見逃す危険があります。

---

## 3. CSVファイルの設計 — グリッドマップ

今回の `Resources/enemy_data.csv` は次のような形式です。

```csv
,,,,,,,,
,e,,,,,,,
,,,,,,,,
,,,,e,,,,
,,,,,,,,
,,,,,,,,e
...
```

各セルがゲームフィールド上の1マスを表しています。`e` が書かれているマスに敵を配置します。

```
列  0  1  2  3  4  5  6  7  8
行0  .  .  .  .  .  .  .  .  .
行1  .  e  .  .  .  .  .  .  .   ← 列1 に敵
行2  .  .  .  .  .  .  .  .  .
行3  .  .  .  .  e  .  .  .  .   ← 列4 に敵
行4  .  .  .  .  .  .  .  .  .
行5  .  .  .  .  .  .  .  .  e   ← 列8 に敵
...
```

**ゲームデータを外部ファイルに書き出す利点**

```
コードに直書きした場合
  main.cpp 内：
    enemyManager.Add(..., {  0.0f, 0.0f, -80.0f }, ...);
    enemyManager.Add(..., { 20.0f, 0.0f, -65.0f }, ...);
    enemyManager.Add(..., {-20.0f, 0.0f, -50.0f }, ...);
    ...    ← 敵の配置を変えるたびにコードを修正してビルドが必要

CSVに外部化した場合
  enemy_data.csv を編集するだけでビルド不要
    → ゲームデザイナーがプログラマに頼まずにステージを編集できる
    → ステージ数が増えても main.cpp のコードが増えない
```

---

## 4. `io::LineReader` — 行単位の読み込み

fast-cpp-csv-parser には列数を型引数で指定する `io::CSVReader<N>` と、行を生の文字列で返す `io::LineReader` の2種類があります。

今回のCSVはヘッダ行がなく、行ごとにセル数が変わる可能性があるグリッド形式なので、**`io::LineReader` を使って1行ずつ手動で解析**します。

### io::LineReader の基本的な使い方

```cpp
io::LineReader reader("Resources/enemy_data.csv"); // ファイルを開く

char* line;
while ((line = reader.next_line()) != nullptr) {   // 1行ずつ取得
    // line には ",,e,,,,e,," のような文字列が入っている
}
// ファイルの末尾に達すると next_line() が nullptr を返してループ終了
```

`next_line()` が返す `char*` は **ライブラリ内部のバッファを指すポインタ** です。`new` や `malloc` で確保されたものではないので `delete` は不要です。次の `next_line()` を呼ぶと内容が上書きされるため、保存したい場合は別の変数にコピーします。

---



### 変換式

```cpp
static const float kCellWidth = 5.0f; // 列1つ分の横幅（ユニット）
static const float kCellDepth = 5.0f; // 行1つ分の奥行き（ユニット）
static const int   kColCenter = 4;    // 中央とみなす列番号 (0〜8 の中心)
static const int   kTotalRows = 27;   // CSVの総行数

// X座標：列4を中心(0)として左右に広がる
float x = (col - kColCenter) * kCellWidth;
//  col=0 → x = (0-4)*5 = -20
//  col=4 → x = (4-4)*5 =   0  (中心)
//  col=8 → x = (8-4)*5 = +20

// Z座標：行が大きいほど手前（プレイヤーに近い）
float z = -(kTotalRows - row) * kCellDepth;
//  row=0  → z = -(27-0)*5 = -135  (最奥)
//  row=26 → z = -(27-26)*5 = -5   (最手前)
```

### 全体像

```
          X
  -20     0    +20
   │      │      │
   │      │      │   ← Z = -5  (行26)
   │   [敵]      │
   │      │      │   ← Z = -65 (行13)
   │      │   [敵]
   │      │      │   ← Z = -135 (行0)
   │      │      │
            ↑
         プレイヤー (Z = 0)
```

---

## 5. 実装コード全体

```cpp
// main.cpp（抜粋）

// ① csv.h のインクルード（警告を局所的に抑制して取り込む）
#pragma warning(push)
#pragma warning(disable: 4996)
#include "csv.h"
#pragma warning(pop)

// ② 変換に使う定数
static const float kCellWidth  = 5.0f;  // 列1つ分の横幅
static const float kCellDepth  = 5.0f;  // 行1つ分の奥行き
static const float kEnemyVelZ  = 2.0f;  // 敵が+Z方向へ進む速度
static const int   kColCenter  = 4;     // X軸の中心となる列番号
static const int   kTotalRows  = 27;    // CSVの総行数

// ③ CSVを読み込んで敵を配置する
EnemyManager enemyManager;
{
    io::LineReader reader("Resources/enemy_data.csv"); // ファイルを開く
    int row = 0;
    while (char* line = reader.next_line()) {          // 1行ずつ処理
        int col = 0;
        for (char* p = line; *p; ++p) {                // 1文字ずつ確認
            if (*p == 'e' || *p == 'E') {              // 'e' なら敵を生成
                float x = (col - kColCenter) * kCellWidth;
                float z = -(kTotalRows - row) * kCellDepth;
                enemyManager.Add(dxLibCommon, { x, 0.0f, z }, { 0.0f, 0.0f, kEnemyVelZ });
            } else if (*p == ',') {
                ++col;                                 // カンマで列を進める
            }
        }
        ++row;                                         // 改行で行を進める
    }
}
```

### コードのポイント解説

**`{ }` で囲んだスコープを作る理由**

```cpp
{
    io::LineReader reader("Resources/enemy_data.csv");
    // ... 読み込み処理 ...
}
// ← ここで reader のデストラクタが呼ばれ、ファイルが閉じる
```

ファイルを開いた `reader` は、使い終わったら速やかに閉じるのが作法です。`{ }` で囲むことでブロックを抜けた瞬間にデストラクタが呼ばれ、ファイルハンドルが自動的に解放されます。

**文字列を1文字ずつ走査する方法**

```cpp
for (char* p = line; *p; ++p) {
//           ↑ line の先頭を指す
//                ↑ *p が '\0'（文字列終端）になったらループ終了
//                      ↑ ポインタを1文字分進める
    // *p は現在の文字（例：',' や 'e' や '\0'）
}
```

C++ の文字列（`char*`）はメモリ上に文字が並び、最後に `'\0'`（ヌル文字）が付く形式です。ポインタをインクリメントしながら `'\0'` に到達するまで1文字ずつ処理します。

```
line → [',' ][',' ]['e' ][',' ][',' ]['\0']
         col=0  col=1  col=2  col=3  col=4  終端
                        ↑
                    'e' を発見 → 敵を生成 (col=2)
```

---

