# 第1章（続）Cube同士の当たり判定

## この節で学ぶこと

この節では、2つの Cube が接触しているかどうかを検出する「**当たり判定（コリジョン検出）**」を実装します。

具体的には次の3つのステップで進めます。

1. AABB という手法で2つの Box が重なっているかを数学的に判定する
2. `CollisionManager` クラスを作り、複数の Cube を効率的に管理する
3. `std::function` とラムダ式を使って「衝突時に色を変える」コールバックを登録する

---

## 1-7　当たり判定の基礎：AABB

### 「当たった」とはどういう状態か

2つの箱が「重なっている」かどうかを判定する、最もシンプルな方法が **AABB（Axis-Aligned Bounding Box）** です。

AABB は「辺が X・Y・Z 軸に平行な箱」を想定します。回転している箱の判定は複雑になるため、まずはこちらを使います。

---

### 1次元で考える

3D は難しいので、まず **1次元（直線上）** で考えてみましょう。

```
 Aの範囲         Bの範囲
[------]     [--------]
 ←a_min  a_max→  ←b_min   b_max→

この場合は重なっていない (a_max < b_min)
```

```
 Aの範囲
[----------]
      [----------]
          Bの範囲

この場合は重なっている
```

2つの区間が**重なっている条件**：

```
a_min <= b_max  かつ  b_min <= a_max
```

あるいは逆に「**重なっていない条件**」で考えると分かりやすいです：

```
重なっていない ＝ a_max < b_min  または  b_max < a_min
```

「重なっていない」の逆が「重なっている」です。

---

### 3次元に拡張する

X・Y・Z の**すべての軸で重なっている**ときだけ、2つの箱は衝突しています。

```
X軸でも重なる
AND
Y軸でも重なる
AND
Z軸でも重なる
```

この3条件が揃ったときだけ `true` を返せばよいです。

---

### 中心と半径で表す

コード上は「中心座標（position）と大きさ（localScale）」が手元にあります。これを使って判定式を書き直します。

```
箱Aの中心: a.transform.position.x
箱Aの半径: a.transform.localScale.x * 0.5f  ← スケールの半分

2つの中心の距離: |a.position.x - b.position.x|
2つの半径の合計: halfA + halfB

重なっている ＝ 距離 <= 合計
```

```
     halfA halfB
    [──────┼──────]  [──────┼──────]
           ↑                ↑
         中心A            中心B
           ←────── 距離 ──────→

距離 > halfA + halfB → 重なっていない
距離 <= halfA + halfB → 重なっている
```

これを X・Y・Z 全軸で確認するのが `CheckAABB` 関数です。

---

### CollisionManager.cpp の CheckAABB を読む

```cpp
// CollisionManager.cpp
bool CollisionManager::CheckAABB(const Cube& a, const Cube& b) {
    float halfX = a.transform.localScale.x * 0.5f;
    float halfY = a.transform.localScale.y * 0.5f;
    float halfZ = a.transform.localScale.z * 0.5f;
    float otherHalfX = b.transform.localScale.x * 0.5f;
    float otherHalfY = b.transform.localScale.y * 0.5f;
    float otherHalfZ = b.transform.localScale.z * 0.5f;

    return (fabsf(a.transform.position.x - b.transform.position.x) <= halfX + otherHalfX) &&
           (fabsf(a.transform.position.y - b.transform.position.y) <= halfY + otherHalfY) &&
           (fabsf(a.transform.position.z - b.transform.position.z) <= halfZ + otherHalfZ);
}
```

> **`fabsf` とは**
> `fabsf(x)` は `float` 型の絶対値（マイナスを取り除いた値）を返す関数です。距離は常にプラスである必要があるため使っています。`<cmath>` をインクルードすると使えます。
>
> 例：`fabsf(-3.5f)` → `3.5f`、`fabsf(2.0f)` → `2.0f`

---

## 1-8　CollisionManager クラスの設計

### なぜマネージャクラスが必要か

当たり判定は「**2つの Cube の組み合わせ**」について行います。Cube が2つなら組み合わせは1通りですが、3つなら3通り、4つなら6通りと増えていきます。

```
Cube が増えると組み合わせが増える
  2個 → 1ペア  (A↔B)
  3個 → 3ペア  (A↔B, A↔C, B↔C)
  4個 → 6ペア  (A↔B, A↔C, A↔D, B↔C, B↔D, C↔D)
```

この管理を `main.cpp` に直接書くと複雑になるため、専用の `CollisionManager` クラスに任せます。

---

### CollisionManager.h を読む

```cpp
// CollisionManager.h
#pragma once
#include <vector>

class Cube;  // 前方宣言（Cube.h は含めない）

class CollisionManager {
public:
    void Register(Cube* cube);  // Cube を1つ登録する
    void Update();              // 毎フレーム呼ぶ

private:
    struct Pair {
        Cube* a;
        Cube* b;
        bool wasColliding = false;  // 前フレームで衝突していたか
    };
    std::vector<Cube*> cubes_;  // 登録された全 Cube
    std::vector<Pair>  pairs_;  // 全ペアのリスト

    static bool CheckAABB(const Cube& a, const Cube& b);
};
```

#### `struct Pair` の役割

`Pair` はCube2個のペアを1つにまとめた構造体です。`wasColliding` が重要で、「**前のフレームで衝突していたか**」を記録します。

```
前フレーム → 今フレーム → イベント
─────────────────────────────────────
false     → true      → onCollisionEnter  (衝突開始)
true      → true      → onCollisionStay   (衝突継続)
true      → false     → onCollisionExit   (衝突終了)
false     → false     → （何もなし）
```

この3種類のイベントは Unity の `OnCollisionEnter` / `OnCollisionStay` / `OnCollisionExit` と同じ考え方です。

---

### Register を読む

```cpp
// CollisionManager.cpp
void CollisionManager::Register(Cube* cube) {
    for (Cube* existing : cubes_) {
        pairs_.push_back({ existing, cube, false });  // 既存の全 Cube とペアを作る
    }
    cubes_.push_back(cube);
}
```

`Register` を呼ぶたびに、**すでに登録済みの全 Cube とのペアを自動生成**してから自分を追加します。

```
Register(A) → cubes_=[A],  pairs_=[]
Register(B) → cubes_=[A,B], pairs_=[(A,B)]
Register(C) → cubes_=[A,B,C], pairs_=[(A,B),(A,C),(B,C)]
```

---

### Update を読む

```cpp
// CollisionManager.cpp
void CollisionManager::Update() {
    for (auto& pair : pairs_) {
        bool isColliding = CheckAABB(*pair.a, *pair.b);

        if (isColliding && !pair.wasColliding) {
            if (pair.a->onCollisionEnter) pair.a->onCollisionEnter(pair.b->gameObject);
            if (pair.b->onCollisionEnter) pair.b->onCollisionEnter(pair.a->gameObject);
        } else if (isColliding && pair.wasColliding) {
            if (pair.a->onCollisionStay) pair.a->onCollisionStay(pair.b->gameObject);
            if (pair.b->onCollisionStay) pair.b->onCollisionStay(pair.a->gameObject);
        } else if (!isColliding && pair.wasColliding) {
            if (pair.a->onCollisionExit) pair.a->onCollisionExit(pair.b->gameObject);
            if (pair.b->onCollisionExit) pair.b->onCollisionExit(pair.a->gameObject);
        }

        pair.wasColliding = isColliding;
    }
}
```

毎フレームすべてのペアを走査し、今フレームの衝突状態と前フレームの状態を比較してイベントを発火します。最後に `pair.wasColliding = isColliding` で「今フレームの状態」を次フレームへ引き継ぎます。

> **`if (pair.a->onCollisionEnter)` の意味**
> `onCollisionEnter` が「空（未設定）」の場合でも安全に呼び出せるよう、先にチェックしています。`std::function` は未設定のとき `false` と評価されます。

---

## 1-9　コールバックと std::function・ラムダ式

### コールバックとは

「何か出来事が起きたときに呼び出してほしい関数」を登録しておく仕組みを**コールバック**と呼びます。

```
使う側（main.cpp）    ライブラリ側（CollisionManager）
     │                           │
     │── onCollisionEnter に ───→│
     │   「色を変える処理」を    │
     │   登録しておく            │
     │                           │
     │           衝突検出 ───────│
     │                           │── 登録した処理を呼ぶ
     │← 色が変わる ──────────────│
```

`CollisionManager` は「いつ呼ぶか」だけを知っていればよく、「何をするか」は登録した側が自由に決められます。

---

### std::function とは

`std::function<void(GameObject)>` は「引数が `GameObject` 1個で戻り値が `void` の関数」を格納できる型です。`<functional>` のインクルードが必要です。

```cpp
// Cube.h
#include <functional>

class Cube {
public:
    // ...
    std::function<void(GameObject)> onCollisionEnter; // 衝突開始フレーム
    std::function<void(GameObject)> onCollisionStay;  // 衝突継続中の毎フレーム
    std::function<void(GameObject)> onCollisionExit;  // 衝突終了フレーム
    // ...
};
```

普通の関数ポインタと違い、`std::function` は**ラムダ式**（後述）を格納できます。

---

### ラムダ式とは

**ラムダ式**は「名前のない使い捨ての関数」を書く構文です。関数を変数に代入したいときや、コールバックに渡したいときに使います。

```cpp
// 通常の関数
void PrintHello() {
    // ...
}

// ラムダ式（同じことをインラインで書く）
auto hello = []() { /* ... */ };
```

基本的な書き方：

```
[ キャプチャ ] ( 引数 ) { 処理 }
```

| 部分 | 意味 |
|---|---|
| `[]` | キャプチャ（外側の変数を使うかどうか） |
| `()` | 関数の引数 |
| `{}` | 関数の本体 |

---

### キャプチャ `[&]` とは

ラムダ式の中で外側の変数を使いたい場合、**キャプチャ**を指定します。

```cpp
int x = 10;

// キャプチャなし → x は使えない（コンパイルエラー）
auto f1 = []() { /* x が使えない */ };

// [&] → 外側の全変数を参照で取り込む
auto f2 = [&]() { x = 20; };  // x を変更できる
```

`[&]` は「外側のスコープにあるすべての変数を**参照（参照渡し）**でキャプチャ」します。参照なので、ラムダ式の中で値を変更すると外側の変数も変わります。

---

### main.cpp のコールバック登録を読む

```cpp
// main.cpp
unsigned int hitColor    = dxLibCommon->MakeColor(255, 50, 50);  // 赤色
unsigned int defaultColor  = cube.color;
unsigned int defaultColor2 = cube2.color;

// cube の衝突コールバックを登録
cube.onCollisionEnter = [&](GameObject other) { cube.color  = hitColor;     };
cube.onCollisionExit  = [&](GameObject other) { cube.color  = defaultColor; };

// cube2 の衝突コールバックを登録
cube2.onCollisionEnter = [&](GameObject other) { cube2.color = hitColor;      };
cube2.onCollisionExit  = [&](GameObject other) { cube2.color = defaultColor2; };
```

`[&]` で `cube`・`cube2`・`hitColor`・`defaultColor` をすべてキャプチャしているため、ラムダの中からこれらの変数を自由に変更できます。

引数 `other` は「衝突した相手の `GameObject`」ですが、この例では使っていません。将来「相手のタグが `"Enemy"` のときだけ反応する」といった処理に使えます。

---

### main.cpp への組み込み全体

```cpp
// main.cpp（変更箇所のみ抜粋）
#include "CollisionManager.h"  // ← 追加

// キューブ1（矢印キーで操作）
Cube cube;
cube.transform.position   = { 0.0f, 0.0f, 0.0f };
cube.transform.localScale = { 2.0f, 2.0f, 2.0f };
cube.Initialize(dxLibCommon);
unsigned int defaultColor = cube.color;

// キューブ2（固定）
Cube cube2;
cube2.transform.position   = { 4.0f, 0.0f, 0.0f };
cube2.transform.localScale = { 2.0f, 2.0f, 2.0f };
cube2.Initialize(dxLibCommon);
unsigned int defaultColor2 = cube2.color;

unsigned int hitColor = dxLibCommon->MakeColor(255, 50, 50);  // 赤

// コールバック登録
cube.onCollisionEnter  = [&](GameObject other) { cube.color  = hitColor;     };
cube.onCollisionExit   = [&](GameObject other) { cube.color  = defaultColor; };
cube2.onCollisionEnter = [&](GameObject other) { cube2.color = hitColor;      };
cube2.onCollisionExit  = [&](GameObject other) { cube2.color = defaultColor2; };

// CollisionManager に登録
CollisionManager collisionManager;
collisionManager.Register(&cube);
collisionManager.Register(&cube2);

while (dxLibCommon->Running())
{
    // ... キー入力・カメラ更新 ...

    collisionManager.Update();   // ← ループ内で毎フレーム呼ぶ

    dxLibCommon->PreDraw();
    dxLibCommon->SetCamera(cameraPos, { 0.0f, 0.0f, 0.0f });
    cube.Draw(dxLibCommon);
    cube2.Draw(dxLibCommon);     // ← 追加
    dxLibCommon->PostDraw();
}
```

**処理の流れ（毎フレーム）**

```
① 矢印キーで cube を移動
② WASD でカメラを回転
③ collisionManager.Update()
     └─ CheckAABB で重なり検出
     └─ 状態が変化したら onCollisionEnter / Exit を呼ぶ
         └─ ラムダ式が実行される → cube.color が変わる
④ 描画
```

---

### プロジェクトへの登録

新しいファイル `CollisionManager.cpp` / `CollisionManager.h` を追加したので、プロジェクトファイルにも登録が必要です。

```xml
<!-- 2025DxLibSotuken.vcxproj -->
<ItemGroup>
  <ClCompile Include="CollisionManager.cpp" />   <!-- ← 追加 -->
  <ClCompile Include="Cube.cpp" />
  ...
</ItemGroup>
<ItemGroup>
  <ClInclude Include="CollisionManager.h" />     <!-- ← 追加 -->
  <ClInclude Include="Cube.h" />
  ...
</ItemGroup>
```

---

## まとめ

| 概念 | 今回の役割 |
|---|---|
| AABB | 各軸の重なりをすべて満たせば衝突と判定する |
| `CollisionManager` | Cube ペアを管理し、毎フレーム衝突状態を更新する |
| `wasColliding` フラグ | 前フレームの状態と比較して Enter / Stay / Exit を区別する |
| `std::function` | 「あとで呼ぶ関数」を変数として格納できる型 |
| ラムダ式 `[&](引数){ 処理 }` | 使い捨ての小さな関数をその場に書く構文 |
| `[&]` キャプチャ | 外側の変数を参照で取り込み、ラムダ内から変更できる |

**この節の設計ポイント**

1. `CheckAABB` は3軸すべての重なりを `&&` で繋げて判定する
2. `wasColliding` フラグで Enter / Stay / Exit の3フェーズを区別する
3. `onCollisionEnter` などは `std::function` で保持し、登録されているときだけ呼ぶ
4. `main.cpp` 側はラムダ式でコールバックを登録するだけでよく、`CollisionManager` の内部実装を知る必要がない

---
