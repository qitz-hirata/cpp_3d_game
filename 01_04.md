# 01_04 プレイヤーの実装 〜処理の移管・定数・エッジ検出〜

## 今回学ぶこと

| # | テーマ |
|---|---|
| 1 | `enum` に値を追加する（`GameKey::Space`） |
| 2 | **前向き宣言**（Forward Declaration） — インクルードを最小限にする |
| 3 | **`static constexpr`** — クラスに属する定数の書き方 |
| 4 | **エッジ検出** — キーを「押した瞬間だけ」検知する仕組み |
| 5 | **処理の移管** — main.cpp から Player クラスへ責務を移す |
| 6 | リファクタリング — コードを整理して main.cpp を読みやすくする |

---

## 1. `GameKey` enum の拡張 — Space キーの追加

### 変更前 / 変更後

```cpp
// 変更前（DxLibCommon.h）
enum class GameKey { Left, Right, Up, Down, W, A, S, D };

// 変更後
enum class GameKey { Left, Right, Up, Down, W, A, S, D, Space }; // 末尾に追加
```

```cpp
// DxLibCommon.cpp の IsKeyDown に追記
case GameKey::Space: return CheckHitKey(KEY_INPUT_SPACE) != 0;
```

**ポイント：`enum class` への値の追加**

`enum class` はカンマ区切りで値を追加できます。
既存の値の順番（内部的な番号）は変わらないため、
**末尾に追加するのが安全**です。途中に挿入すると
既存の番号がずれてバグの原因になります。

```cpp
// 悪い例：途中に挿入（既存の値の番号がずれる）
enum class GameKey { Left, Right, Space, Up, Down, W, A, S, D };
//                               ↑ここに入れると Right=1, Space=2, Up=3... にずれる

// 良い例：末尾に追加（既存の番号は変わらない）
enum class GameKey { Left, Right, Up, Down, W, A, S, D, Space };
```

---

## 2. Player クラスの設計（Player.h）

### 完成形

```cpp
// Player.h
#pragma once
#include "Cube.h"

class DxLibCommon;   // 前向き宣言
class BulletManager; // 前向き宣言

class Player {
public:
    Cube cube; // 見た目・位置・大きさを持つ箱

    void Initialize(DxLibCommon* dxLib);
    void Update(DxLibCommon* dxLib, BulletManager& bulletManager, float deltaTime);
    void Draw(DxLibCommon* dxLib) const;

private:
    bool spaceWasDown_ = false; // 前フレームでスペースキーが押されていたか

    static constexpr float kMoveSpeed   = 3.0f;  // 移動速度（ユニット/秒）
    static constexpr float kBulletSpeed = 10.0f; // 弾速（ユニット/秒）
    static constexpr float kBulletLife  = 3.0f;  // 弾の生存時間（秒）
};
```

---

## 3. 前向き宣言（Forward Declaration）

### インクルードとの違い

```cpp
// ① インクルード（ヘッダファイルの内容を全部読み込む）
#include "DxLibCommon.h"  // DxLibCommon クラスの全定義が展開される

// ② 前向き宣言（「このクラスは存在する」とだけ伝える）
class DxLibCommon;        // 中身は教えない
```

### なぜ前向き宣言を使うのか

`Player.h` では `DxLibCommon` をポインタ（`DxLibCommon*`）や
参照（`BulletManager&`）としてしか使っていません。
ポインタ・参照はどのクラスでもサイズが同じ（8バイト）なので、
クラスの中身を知らなくても宣言できます。

```
インクルード = 「DxLibCommon の設計書を全部ここに貼り付けて」
前向き宣言  = 「DxLibCommon というクラスがあることだけ覚えておいて」
```

**メリット**

| 比較 | インクルード | 前向き宣言 |
|------|------------|----------|
| ビルド速度 | 遅くなる（ヘッダが多いほど） | 速い |
| 依存関係 | 増える | 最小限 |
| 使える場面 | 中身（メソッド・メンバ）を使う場合 | ポインタ・参照だけの場合 |

> **ルール：ヘッダファイル（.h）では前向き宣言を使い、**
> **実装ファイル（.cpp）でインクルードする**

```cpp
// Player.h  → 前向き宣言だけ
class DxLibCommon;

// Player.cpp → 実際に使うので #include
#include "DxLibCommon.h"
#include "BulletManager.h"
```

---

## 4. `static constexpr` — クラスに属する定数

### 書き方

```cpp
private:
    static constexpr float kMoveSpeed   = 3.0f;
    static constexpr float kBulletSpeed = 10.0f;
    static constexpr float kBulletLife  = 3.0f;
```

**3つのキーワードの意味**

| キーワード | 意味 |
|-----------|------|
| `static` | このクラスの **全インスタンスで共有**（Player が何体いても 1 つだけ存在） |
| `constexpr` | **コンパイル時に値が確定**（実行中に変わらない、処理速度が上がる） |
| `float` | 浮動小数点数型 |

### `static` がなかった場合との比較

```cpp
// static なし → Player 1体ごとに kMoveSpeed が別々にメモリに確保される（無駄）
float kMoveSpeed = 3.0f; // Player が 10 体いたら 10 個分のメモリ

// static あり → Player クラスに 1 つだけ存在（共有）
static constexpr float kMoveSpeed = 3.0f; // 何体いても 1 個分のメモリ
```

### `constexpr` がなかった場合との比較

```cpp
// const のみ → 実行時に値が決まる（わずかに遅い可能性）
static const float kMoveSpeed = 3.0f;

// constexpr → コンパイル時に値が決まる（より最適化される）
static constexpr float kMoveSpeed = 3.0f;
```

### なぜ `k` で始まるのか

慣習的に **定数（Konstante = ドイツ語で定数）** の頭文字 `k` を付けて
変数と区別します。このプロジェクト全体で使われている命名規則です。

```cpp
static constexpr float kMoveSpeed = 3.0f; // 定数 → k で始まる
float someVariable = 0.0f;               // 変数 → k なし
```

---

## 5. Player::Initialize — 初期化処理

```cpp
// Player.cpp
void Player::Initialize(DxLibCommon* dxLib) {
    cube.transform.localScale = { 2.0f, 2.0f, 2.0f }; // 大きさを設定
    cube.color = dxLib->MakeColor(255, 200, 100);      // 黄色がかったオレンジ
}
```

初期位置は `main.cpp` 側で設定しています。
**「どこに生成するか」は使う側（main）が決め、
「初期化の中身」はクラス（Player）が管理する** という分け方です。

```cpp
// main.cpp 側
Player player;
player.cube.transform.position = { 0.0f, 0.0f, 0.0f }; // 位置はここで指定
player.Initialize(dxLibCommon);                          // 色・大きさはクラスが決める
```

---

## 6. Player::Update — 移動と発射

```cpp
// Player.cpp
void Player::Update(DxLibCommon* dxLib, BulletManager& bulletManager, float deltaTime) {

    // --- 移動 ---
    if (dxLib->IsKeyDown(GameKey::Left))  { cube.transform.position.x -= kMoveSpeed * deltaTime; }
    if (dxLib->IsKeyDown(GameKey::Right)) { cube.transform.position.x += kMoveSpeed * deltaTime; }
    if (dxLib->IsKeyDown(GameKey::Up))    { cube.transform.position.z += kMoveSpeed * deltaTime; }
    if (dxLib->IsKeyDown(GameKey::Down))  { cube.transform.position.z -= kMoveSpeed * deltaTime; }

    // --- 発射（押した瞬間だけ）---
    bool spaceDown = dxLib->IsKeyDown(GameKey::Space);
    if (spaceDown && !spaceWasDown_) {
        bulletManager.Add(dxLib, cube.transform.position, BulletOwner::Ally,
                          { 0.0f, 0.0f, -kBulletSpeed }, kBulletLife);
    }
    spaceWasDown_ = spaceDown; // 今フレームの状態を保存して次フレームへ
}
```

### 6-1. 移動処理

前回の資料（01_03）で学んだ **デルタタイム** を活用しています。
`kMoveSpeed × deltaTime` により、フレームレートに依存しない移動を実現しています。

```
移動量 = 速度（ユニット/秒） × 経過時間（秒）
```

---

### 6-2. エッジ検出 —「押した瞬間だけ」を検知する

**問題：キーを 1 秒押し続けたら何が起きるか？**

```
IsKeyDown は「今押しているか」を返すだけ

60fps のとき、1 秒間スペースを押し続けると
→ 60フレーム × 毎フレーム発射 = 60 発も出てしまう！
```

**解決策：前フレームとの比較（エッジ検出）**

```
「押した瞬間」= 今フレームが ON かつ 前フレームが OFF
```

```cpp
bool spaceDown = dxLib->IsKeyDown(GameKey::Space); // 今フレームの状態

// ↓ 「今 ON」かつ「前フレームは OFF」のときだけ発射
if (spaceDown && !spaceWasDown_) {
    // 弾を生成
}

spaceWasDown_ = spaceDown; // 今の状態を「前フレームの状態」として保存
```

**フレームごとの変化を図で見る**

```
フレーム:       1    2    3    4    5    6
スペースキー:  OFF  ON   ON   ON   OFF  ON
spaceWasDown_: OFF  OFF  ON   ON   ON   OFF
                    ↑              ↑
              発射する！        発射する！
             （ON かつ前がOFF）  （ON かつ前がOFF）
```

`spaceWasDown_` は `private` メンバ変数です。
「前フレームの状態を記憶する」という **Player 内部の状態** なので、
外から見えなくて良い情報として隠しています。

---

### 6-3. 弾の生成

```cpp
bulletManager.Add(
    dxLib,
    cube.transform.position,       // プレイヤーの現在位置から発射
    BulletOwner::Ally,             // 味方の弾
    { 0.0f, 0.0f, -kBulletSpeed }, // -Z 方向（前方）へ
    kBulletLife                    // 3秒後に消える
);
```

**弾はプレイヤーが持つのではなく BulletManager に委ねる**

プレイヤーが弾を `std::vector<Bullet>` として持つ設計も考えられますが、
BulletManager に渡すことで以下のメリットがあります。

```
プレイヤーが弾を持つ設計
  Player ─ vector<Bullet>   ← プレイヤーが管理

BulletManager に委ねる設計
  Player ────────────────→ BulletManager ─ vector<Bullet>
         Add() を呼ぶだけ               ← Manager が管理

メリット：敵が弾を撃つときも同じ BulletManager を使える
         弾の Update/Draw を一か所で管理できる
```

---

## 7. main.cpp の変化

### 7-1. 初期化部分の変化

```cpp
// --- 変更前（main.cpp が全部知っていた）---
Cube cube;
cube.transform.position   = { 0.0f, 0.0f, 0.0f };
cube.transform.localScale = { 2.0f, 2.0f, 2.0f };  // ← サイズを main が指定
cube.Initialize(dxLibCommon);
unsigned int defaultColor = cube.color;

// --- 変更後（Player クラスに委ねた）---
Player player;
player.cube.transform.position = { 0.0f, 0.0f, 0.0f }; // 位置だけ指定
player.Initialize(dxLibCommon);                          // サイズ・色は Player が決める
unsigned int defaultPlayerColor = player.cube.color;
```

### 7-2. メインループの変化

```cpp
// --- 変更前（5行のキー入力処理が main にあった）---
if (dxLibCommon->IsKeyDown(GameKey::Left))  { cube.transform.position.x -= kMoveSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::Right)) { cube.transform.position.x += kMoveSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::Up))    { cube.transform.position.z += kMoveSpeed; }
if (dxLibCommon->IsKeyDown(GameKey::Down))  { cube.transform.position.z -= kMoveSpeed; }

// --- 変更後（1行に集約）---
player.Update(dxLibCommon, bulletManager, deltaTime);
```

**Before/After の全体比較**

| 処理 | 変更前（main.cpp） | 変更後（Player.cpp） |
|------|------------------|-------------------|
| 大きさの設定 | main.cpp が直接書く | `Initialize` の中 |
| 移動キー入力 | main.cpp のループ内 | `Update` の中 |
| 発射キー入力 | なし（未実装） | `Update` の中 |
| デルタタイム掛け算 | なし（フレーム依存） | `Update` の中 |

---

## 8. 設計の考え方 — 責務の分離（Single Responsibility）

今回の変更の本質は「**プレイヤーに関することはすべて Player クラスが責任を持つ**」
という設計思想です。

```
変更前：main.cpp がプレイヤーのことを「全部知っている」
  main.cpp
  ├─ プレイヤーの大きさを設定する
  ├─ プレイヤーを移動させる（キー入力）
  └─ プレイヤーを描画する

変更後：main.cpp は「存在を知っているだけ」で良い
  main.cpp
  ├─ player.Initialize()  ← 任せる
  ├─ player.Update()      ← 任せる
  └─ player.Draw()        ← 任せる

  Player クラス が全部知っている
  ├─ 自分の大きさは 2.0f × 2.0f × 2.0f
  ├─ 移動速度は 3.0f ユニット/秒
  ├─ スペースで弾を撃つ
  └─ 弾速は 10.0f ユニット/秒
```

**なぜこれが重要か？**

- **変更しやすい** ── 移動速度を変えたいなら `Player.h` の `kMoveSpeed` だけ変えればよい
- **読みやすい** ── main.cpp を見れば「何が存在しているか」だけわかる
- **再利用しやすい** ── Player クラスを別のゲームに持っていけば、そのまま使える

---

## 9. クラス・ファイルの関係図

```
Player.h
  │  #include "Cube.h"       ← Cube の定義が必要（メンバとして持つため）
  │  class DxLibCommon;      ← 前向き宣言（ポインタで使うだけ）
  │  class BulletManager;    ← 前向き宣言（参照で使うだけ）
  └─ Player クラス定義

Player.cpp
  │  #include "Player.h"
  │  #include "DxLibCommon.h"  ← IsKeyDown などを実際に使うので必要
  │  #include "BulletManager.h" ← Add() を実際に呼ぶので必要
  └─ Initialize / Update / Draw の実装

main.cpp
  │  #include "Player.h"
  └─ Player を生成して Update/Draw を呼ぶだけ
```

---

## 10. 処理フロー まとめ

```
毎フレーム（ゲームループ）

1. player.Update(dxLibCommon, bulletManager, deltaTime)
   │
   ├─ 矢印キー押下チェック
   │     └─ 各方向に kMoveSpeed × deltaTime 分だけ移動
   │
   └─ スペースキーのエッジ検出
         今フレーム ON かつ 前フレーム OFF？
           ├─ YES → bulletManager.Add() で弾を 1 発追加
           └─ NO  → 何もしない
         → 今フレームの状態を spaceWasDown_ に保存

2. bulletManager.Update(deltaTime)
   └─ 追加された弾を移動・寿命管理

3. player.Draw(dxLibCommon)
   └─ プレイヤーの Cube を描画
```

---

## 11. チェックリスト（理解度確認）

- [ ] `enum class` に値を安全に追加する方法を説明できる
- [ ] 前向き宣言を使う理由とメリットを説明できる
- [ ] `static constexpr` の `static` と `constexpr` それぞれの意味を説明できる
- [ ] エッジ検出（押した瞬間だけ検知）の仕組みを図で説明できる
- [ ] `spaceWasDown_` が `private` である理由を説明できる
- [ ] main.cpp からクラスへ処理を移管するメリットを説明できる
- [ ] `class BulletManager;`（前向き宣言）と `#include "BulletManager.h"` の使い分けができる

---

## 12. 演習課題

**基礎**
1. `kMoveSpeed` の値を変えて、プレイヤーの移動速度が変わることを確認しよう。

2. スペースキーだけでなく `GameKey::A` でも弾が撃てるように
   `Player::Update` を変更してみよう。

**応用**
3. プレイヤーが弾を撃ったあと、一定時間（例：0.5 秒）は連射できない
   **クールタイム** を実装してみよう。  
   ヒント：`float coolTime_ = 0.0f;` というメンバ変数を追加し、
   発射のたびにセットして毎フレーム `deltaTime` 分だけ減らす。

4. プレイヤーが画面端（例：X 軸が -5.0f 〜 5.0f の範囲外）に
   出られないよう制限を加えてみよう。  
   ヒント：`std::clamp(値, 最小, 最大)` を使う。
