# 01_03 弾と敵の実装 〜クラスとマネージャーパターン〜

## 今回学ぶこと

| # | テーマ |
|---|---|
| 1 | `enum class` で「所属」を型安全に表現する |
| 2 | クラスに **生存フラグ** と **寿命** を持たせる |
| 3 | **デルタタイム** を使ったフレームレートに依存しない移動 |
| 4 | `std::vector` で複数のオブジェクトをまとめて管理する |
| 5 | **Manager パターン** —— クラスの Update/Draw を一か所で呼ぶ設計 |
| 6 | **erase-remove イディオム** —— 死んだオブジェクトを安全に消す |
| 7 | **AABB 当たり判定** —— 箱同士が重なっているかを数学で調べる |

---

## 1. 弾（Bullet）クラス

### 1-1. `BulletOwner` — 弾の「所属」を enum class で表す

```cpp
// Bullet.h
enum class BulletOwner { Ally, Enemy };
```

**なぜ `enum class`（スコープ付き列挙型）を使うのか？**

昔ながらの `enum` は値がそのまま `int` に変換され、
意図しない比較ができてしまいます。`enum class` を使うと
**型が厳密に区別される** ため、バグを防ぎやすくなります。

```cpp
// 悪い例：普通の enum
enum Owner { Ally, Enemy };
int x = Ally;  // int に勝手に変換される（意図しない代入ができる）

// 良い例：enum class
enum class BulletOwner { Ally, Enemy };
// int x = BulletOwner::Ally;  コンパイルエラー！ 型安全
```

---

### 1-2. Bullet クラスの設計

```cpp
// Bullet.h
class Bullet {
public:
    Cube        cube;                          // 見た目（箱）
    Vector3     velocity = { 0.0f, 0.0f, 0.0f }; // 移動速度（方向 × 速さ）
    BulletOwner owner    = BulletOwner::Ally; // 誰の弾か

    void Initialize(DxLibCommon* dxLib, BulletOwner ownerType, Vector3 vel, float lifetime);
    void Update(float deltaTime);
    void Draw(DxLibCommon* dxLib) const;
    bool IsAlive() const;
    void Kill();           // 外部から強制消滅

private:
    float lifetime_ = 0.0f; // 残り生存時間（秒）
    bool  isAlive_  = false; // 生きているか
};
```

**クラスの責務（役割）を整理する**

```
Bullet が持つもの
├─ cube       → 3D空間での見た目・位置・大きさ
├─ velocity   → どの方向にどれだけ速く動くか
├─ owner      → 味方の弾か敵の弾か
├─ lifetime_  → あと何秒で消えるか（外からは変更させない → private）
└─ isAlive_   → 今生きているか（同上）
```

> **private のメンバ変数に `_` を付ける慣習**
> このプロジェクトでは `lifetime_` `isAlive_` のように末尾アンダースコアを
> 付けることで「外から直接触るな」と示しています。

---

### 1-3. Initialize — 初期化処理

```cpp
// Bullet.cpp
void Bullet::Initialize(DxLibCommon* dxLib, BulletOwner ownerType, Vector3 vel, float lifetime) {
    owner     = ownerType;
    velocity  = vel;
    lifetime_ = lifetime;
    isAlive_  = true;

    cube.transform.localScale = { 0.3f, 0.3f, 0.3f }; // 小さな箱

    // 味方弾は水色、敵弾は赤色
    cube.color = (owner == BulletOwner::Ally)
        ? dxLib->MakeColor(100, 200, 255)
        : dxLib->MakeColor(255, 80,  80);
}
```

三項演算子 `条件 ? 値A : 値B` は「条件が true なら A、false なら B」を返す短縮記法です。

---

### 1-4. デルタタイム（DeltaTime）— フレームレートに依存しない移動

**デルタタイムとは？**

ゲームは「1秒間に何十回もループが回る」仕組みです。
PC の性能によって 1 秒間のループ回数（フレームレート）が変わるため、
「1フレームで何ユニット動かす」と書くと速い PC では速く、遅い PC では遅くなります。

```
問題：フレームレートが違うと動く距離が変わる
  60fps の PC → 1秒で 60回 × 0.05 = 3.0 ユニット移動
  30fps の PC → 1秒で 30回 × 0.05 = 1.5 ユニット移動  ← 遅い！
```

**解決策：「前のフレームからの経過時間（デルタタイム）」を掛ける**

```cpp
// main.cpp でデルタタイムを計算
auto now = std::chrono::steady_clock::now();
float deltaTime = std::chrono::duration<float>(now - prevTime).count(); // 秒単位
prevTime = now;
```

```cpp
// Bullet::Update の中
cube.transform.position.x += velocity.x * deltaTime; // 速度 × 経過秒数
cube.transform.position.y += velocity.y * deltaTime;
cube.transform.position.z += velocity.z * deltaTime;
```

これにより **どんな PC でも 1 秒あたりの移動量が同じ** になります。

```
60fps の PC → deltaTime ≈ 0.0167秒 → 速度5 × 0.0167 ≈ 0.083ユニット/フレーム
30fps の PC → deltaTime ≈ 0.0333秒 → 速度5 × 0.0333 ≈ 0.167ユニット/フレーム
どちらも 1秒後には 5ユニット移動している ✓
```

---

### 1-5. 寿命管理 — 時間が来たら自動で消える

```cpp
void Bullet::Update(float deltaTime) {
    if (!isAlive_) return; // 死んでいたら何もしない

    // 位置を更新（上述）

    lifetime_ -= deltaTime;       // 残り時間を減らす
    if (lifetime_ <= 0.0f) {
        isAlive_ = false;          // 寿命が来たら死亡
    }
}
```

**IsAlive と Kill**

```cpp
bool Bullet::IsAlive() const { return isAlive_; }
void Bullet::Kill()           { isAlive_ = false; } // 外部から強制消滅させる用
```

`Kill()` は後述の「敵に当たったとき」に BulletManager から呼びます。

---

## 2. BulletManager クラス

### 2-1. なぜ Manager が必要か？

弾は同時に何発も飛びます。main.cpp で直接 `Bullet bullet1; Bullet bullet2; ...` と
書くと管理が大変になります。**Manager クラス** を作って、弾の生成・更新・描画を
一か所にまとめます。

```
main.cpp から見た景色

Before（Manager なし）    After（Manager あり）
  bullet1.Update(dt)        bulletManager.Update(dt)  ← 1行で全部！
  bullet2.Update(dt)
  bullet3.Update(dt)
  ...
```

---

### 2-2. `std::vector` で弾をまとめて管理する

```cpp
// BulletManager.h
class BulletManager {
public:
    void Add(DxLibCommon* dxLib, Vector3 position, BulletOwner owner,
             Vector3 velocity, float lifetime);
    void Update(float deltaTime);
    void Draw(DxLibCommon* dxLib) const;
    bool CheckAndKillAllyHit(const Cube& target);

private:
    std::vector<Bullet> bullets_; // 弾を何発でも入れられる配列
};
```

`std::vector` は **可変長の配列** です。`push_back()` で末尾に追加でき、
サイズを気にせず使えます。

---

### 2-3. Add — 弾を追加する

```cpp
void BulletManager::Add(DxLibCommon* dxLib, Vector3 position, BulletOwner owner,
                         Vector3 velocity, float lifetime) {
    Bullet bullet;
    bullet.cube.transform.position = position;
    bullet.Initialize(dxLib, owner, velocity, lifetime);
    bullets_.push_back(std::move(bullet)); // vector に追加
}
```

**`std::move` とは？**

`push_back(bullet)` と書くと `bullet` の**コピー**が vector に入ります。
`push_back(std::move(bullet))` と書くと `bullet` の**中身をそのまま移動**するため、
無駄なコピーが発生しません（所有権の移動）。

```
コピー：元のデータも、vector 内のデータも両方存在する（メモリ無駄）
move  ：元の変数は空になり、vector 内にデータが移動する（効率的）
```

---

### 2-4. Update — 全弾を更新し、死んだ弾を削除する

```cpp
void BulletManager::Update(float deltaTime) {
    // 全弾を更新
    for (auto& bullet : bullets_) {
        bullet.Update(deltaTime);
    }

    // 死んだ弾を vector から取り除く（erase-remove イディオム）
    bullets_.erase(
        std::remove_if(bullets_.begin(), bullets_.end(),
            [](const Bullet& b) { return !b.IsAlive(); }),
        bullets_.end()
    );
}
```

**erase-remove イディオム とは？**

vector から条件に合う要素を削除するときの定番パターンです。

```
bullets_ = [弾A(生存), 弾B(死亡), 弾C(生存), 弾D(死亡)]

1. std::remove_if → 生存している弾を前に詰める
   [弾A, 弾C, ゴミ, ゴミ]   ← 後ろにゴミが残る
                    ↑ここ以降を消す境界（iterator）を返す

2. erase → 境界以降を完全に削除
   [弾A, 弾C]
```

> **なぜループ中に直接 erase しないのか？**
> ループ中に vector のサイズを変えると、イテレータ（位置を指す印）が
> 壊れてクラッシュします。erase-remove イディオムはこれを安全に回避します。

**ラムダ式 `[](const Bullet& b) { return !b.IsAlive(); }`**

無名の関数（ラムダ）を引数として渡しています。
「各 `b`（弾）が生きていなければ `true` を返す」 = 「死んでいたら削除対象」という意味です。

---

### 2-5. Draw — 全弾を描画する

```cpp
void BulletManager::Draw(DxLibCommon* dxLib) const {
    for (const auto& bullet : bullets_) {
        bullet.Draw(dxLib); // Bullet::Draw 内で isAlive_ チェックしている
    }
}
```

---

## 3. 敵（Enemy）クラス

Bullet クラスとほぼ同じ構造です。違いを中心に見ていきましょう。

```cpp
// Enemy.h
class Enemy {
public:
    Cube    cube;
    Vector3 velocity = { 0.0f, 0.0f, 0.0f };

    void Initialize(DxLibCommon* dxLib, Vector3 position, Vector3 velocity);
    void Update(float deltaTime);
    void Draw(DxLibCommon* dxLib) const;
    bool IsAlive() const;
    void Kill();

private:
    bool isAlive_ = false; // Bullet と違って寿命（lifetime）はない
};
```

**Bullet との設計上の違い**

| 項目 | Bullet | Enemy |
|------|--------|-------|
| `lifetime_`（寿命） | あり（何秒で消えるか） | なし（撃たれるまで残る） |
| `owner`（所属） | あり（Ally / Enemy） | なし（敵は敵のみ） |
| 消え方 | 寿命か `Kill()` | `Kill()` のみ |

---

## 4. EnemyManager と弾との当たり判定

### 4-1. EnemyManager の構造

```cpp
// EnemyManager.h
class EnemyManager {
public:
    void Add(DxLibCommon* dxLib, Vector3 position, Vector3 velocity);
    void Update(float deltaTime, BulletManager& bulletManager); // ← 弾と衝突判定するため参照を受け取る
    void Draw(DxLibCommon* dxLib) const;

private:
    std::vector<Enemy> enemies_;
};
```

**`BulletManager&`（参照渡し）とは？**

`BulletManager& bulletManager` と書くと、呼び出し元の `bulletManager` を
**コピーせず直接操作**できます。「弾を消す」という副作用を実際の
`bulletManager` に反映させる必要があるため、参照渡しにしています。

```
値渡し（コピー）：中の変更は呼び出し元に伝わらない
参照渡し (&)  ：中の変更が呼び出し元にも反映される  ← こちらを使う
```

---

### 4-2. Update — 移動 + 弾との当たり判定

```cpp
// EnemyManager.cpp
void EnemyManager::Update(float deltaTime, BulletManager& bulletManager) {
    for (auto& enemy : enemies_) {
        if (!enemy.IsAlive()) continue;

        enemy.Update(deltaTime); // 移動

        // 味方弾と当たっているか確認。当たっていたら弾を消して true が返る
        if (bulletManager.CheckAndKillAllyHit(enemy.cube)) {
            enemy.Kill(); // 敵も消す
        }
    }

    // 死んだ敵を削除（Bullet と同じ erase-remove イディオム）
    enemies_.erase(
        std::remove_if(enemies_.begin(), enemies_.end(),
            [](const Enemy& e) { return !e.IsAlive(); }),
        enemies_.end()
    );
}
```

---

### 4-3. AABB 当たり判定の仕組み

**AABB（Axis-Aligned Bounding Box）** とは、軸に平行な箱で当たり判定を行う最もシンプルな手法です。

```
2D で考えるとイメージしやすい

  ┌──────┐
  │  弾  │
  └───┬──┘
      │        ← 2つの箱が X, Y, Z すべての軸で重なっていたら当たり！
  ┌───┴──────┐
  │   敵     │
  └──────────┘
```

コードでは各軸ごとに「中心間の距離 ≦ 2つの半径の合計」をチェックしています。

```cpp
// BulletManager.cpp
bool BulletManager::CheckAndKillAllyHit(const Cube& target) {
    for (auto& bullet : bullets_) {
        if (!bullet.IsAlive() || bullet.owner != BulletOwner::Ally) continue;

        const Cube& bc = bullet.cube;

        // X軸・Y軸・Z軸それぞれで重なりを確認
        bool hit =
            fabsf(bc.transform.position.x - target.transform.position.x)
                <= (bc.transform.localScale.x + target.transform.localScale.x) * 0.5f
            &&
            fabsf(bc.transform.position.y - target.transform.position.y)
                <= (bc.transform.localScale.y + target.transform.localScale.y) * 0.5f
            &&
            fabsf(bc.transform.position.z - target.transform.position.z)
                <= (bc.transform.localScale.z + target.transform.localScale.z) * 0.5f;

        if (hit) {
            bullet.Kill(); // 弾を消す
            return true;   // 当たったことを返す（敵は呼び出し元で消す）
        }
    }
    return false; // 当たらなかった
}
```

**`fabsf`** は浮動小数点の絶対値を返す関数です（負の値を正にする）。

中心間の距離計算を図で表すと：

```
     弾の中心         敵の中心
         │←── 距離 ──→│
  ───┬───┼────────────┼───┬───
  弾の  弾の              敵の  敵の
  左端  右端              左端  右端

  弾の半径 + 敵の半径 ≧ 距離  →  重なっている（当たり！）
```

---

## 5. main.cpp への組み込み方

### 5-1. 初期化部分

```cpp
BulletManager bulletManager;   // 最初は空（弾が入っていない）

EnemyManager enemyManager;
enemyManager.Add(dxLibCommon,
    { 0.0f, 0.0f, -8.0f },    // 初期位置（Z=-8）
    { 0.0f, 0.0f,  2.0f });   // 速度（+Z方向にゆっくり移動）
```

### 5-2. メインループ部分

```cpp
while (dxLibCommon->Running())
{
    // デルタタイムの計算
    auto now = std::chrono::steady_clock::now();
    float deltaTime = std::chrono::duration<float>(now - prevTime).count();
    prevTime = now;

    // --- 更新 ---
    bulletManager.Update(deltaTime);                    // 弾の移動・寿命管理
    enemyManager.Update(deltaTime, bulletManager);      // 敵の移動・弾との衝突

    // --- 描画 ---
    dxLibCommon->PreDraw();
    bulletManager.Draw(dxLibCommon);
    enemyManager.Draw(dxLibCommon);
    dxLibCommon->PostDraw();
}
```

main.cpp は Manager に「任せるだけ」なので、弾が何発あるか・敵が何体いるかを
main.cpp が知る必要はありません。

---

## 6. クラス関係図

```
main.cpp
  │
  ├─ BulletManager ───── std::vector<Bullet>
  │       │                    └─ Cube（描画）
  │       │                    └─ velocity, lifetime_, isAlive_
  │       │
  │       └─ CheckAndKillAllyHit(Cube&)
  │               │ AABB判定して true/false を返す
  │               ↓
  └─ EnemyManager ───── std::vector<Enemy>
            │                └─ Cube（描画）
            │                └─ velocity, isAlive_
            │
            └─ Update(deltaTime, BulletManager&)
                  弾に当たった敵を Kill()
                  → erase-remove で両方消える
```

---

## 7. 処理の流れまとめ

```
毎フレーム（ゲームループ）

1. deltaTime を計算（前のフレームからの経過時間）

2. bulletManager.Update(deltaTime)
   ├─ 各弾の位置を velocity × deltaTime 分だけ移動
   ├─ 各弾の残り寿命を deltaTime 分だけ減らす
   └─ 寿命が尽きた弾を vector から削除

3. enemyManager.Update(deltaTime, bulletManager)
   ├─ 各敵の位置を velocity × deltaTime 分だけ移動
   ├─ 各敵について bulletManager.CheckAndKillAllyHit() を呼ぶ
   │   └─ 味方弾と重なっていたら → 弾を Kill()、true を返す
   ├─ true が返ってきたら → 敵も Kill()
   └─ 死んだ敵を vector から削除

4. 描画
   ├─ bulletManager.Draw()  → 生きている弾を全部描く
   └─ enemyManager.Draw()   → 生きている敵を全部描く
```

---

## 8. チェックリスト（理解度確認）

- [ ] `enum class` と普通の `enum` の違いを説明できる
- [ ] `deltaTime` が必要な理由を説明できる
- [ ] `std::vector` の `push_back` でオブジェクトを追加できる
- [ ] erase-remove イディオムが何をしているか説明できる
- [ ] `std::move` のメリットを説明できる
- [ ] `BulletManager&` が参照渡しである理由を説明できる
- [ ] AABB 当たり判定の式（`fabsf(中心間距離) <= 半径の合計`）を説明できる
- [ ] Manager パターンを使うと main.cpp がどのように変わるか説明できる

---

## 9. 演習課題

**基礎**
1. `BulletOwner::Enemy` の弾（赤い弾）を `BulletManager::Add` で生成し、
   画面上に表示してみよう。

**応用**
2. `EnemyManager` に `AddMultiple(int count, ...)` メソッドを追加し、
   複数の敵を一度に生成できるようにしてみよう。

3. 敵が画面外（例：Z > 10.0f）まで移動したら自動的に `Kill()` されるように
   `Enemy::Update` を改造してみよう。
