# 第7回 データドリブンデザイン ― CSVで敵データを管理しよう

## 今回学ぶこと

今回のコミットでは、ゲームの「敵」をどのように配置するか、そのデータ管理の方法を大きく改善しました。

| 変更前 | 変更後 |
|--------|--------|
| 全ての敵が同じ見た目・同じ速さ | 敵の種類ごとに色・HP・速さが異なる |
| 配置データにしか「e」しか書けない | 配置データに敵の「種類ID」を書ける |
| CSV解析が `main.cpp` に直書き | `EnemyManager` クラスが担当 |

---

## 1. データドリブンデザインとは？

**データドリブンデザイン**とは、ゲームの動作を「プログラム（コード）」ではなく「データ（CSVやJSONなど）」で制御する設計手法です。

### なぜ重要なの？

たとえば、新しい敵の種類を追加したいとき…

**コードに直書きしていると：**
```cpp
// 新しい敵を追加するたびにプログラムを書き換える必要がある
enemyManager.Add(dxLib, {0,0,-30}, {0,0,2});   // 敵タイプA
enemyManager.Add(dxLib, {5,0,-40}, {0,0,3});   // 敵タイプB (HPは？ 色は？)
```
→ プログラムを書き直して、ビルドし直さないといけない

**CSVで管理すると：**
```
enemy_master.csv を編集するだけで OK！
ビルド不要 → プランナーやデザイナーも編集できる
```

---

## 2. 今回使う2つのCSVファイル

### ① マスターデータ `Resources/enemy_master.csv`

「どんな敵の種類があるか」を定義するファイルです。

```
ID,COLOR,HP,SPEED
1,RED,1,5
2,BLUE,3,3
3,YELLOW,5,1
```

| 列 | 意味 |
|----|------|
| ID | 敵の種類番号（配置ファイルで参照する） |
| COLOR | 色（RED / BLUE / YELLOW） |
| HP | 体力（何発で倒せるか） |
| SPEED | 移動速度 |

### ② 配置データ `Resources/enemy_placement.csv`

「どこにどの種類の敵を置くか」を定義するファイルです。

```
,,,,,,,,
,1,,,,,,,     ← 列1にID=1の敵（赤・体力1）
,,,,,,,,
,,,,1,,,,
,,,,,,,,
,,,,,,,,2     ← 列8にID=2の敵（青・体力3）
...
```

- 空白 → 敵なし
- 数字 → その番号の種類の敵を配置
- 行 = Z方向（奥→手前）、列 = X方向（左右）

### 2つを組み合わせる理由

```
マスターデータ（enemy_master.csv）
  ┌─────────────────────────────────┐
  │ ID=1 → 赤・HP1・速さ5          │
  │ ID=2 → 青・HP3・速さ3          │
  │ ID=3 → 黄・HP5・速さ1          │
  └─────────────────────────────────┘
           ↑ 参照
配置データ（enemy_placement.csv）
  「ここにID=1を置く」「あそこにID=2を置く」
```

> **ポイント**: 敵の特性は1か所（マスター）だけに書く。これを**単一責任の原則**と呼びます。
> 「赤い敵のHPを2に変えたい」→ マスターの1行だけ変えればOK！

---

## 3. コードの変更点を読もう

### `EnemyMasterData` 構造体（新規追加）

```cpp
// EnemyManager.h
struct EnemyMasterData {
    int          id;
    unsigned int color;
    int          hp;
    float        speed;
};
```

CSVから読み込んだ1行分のデータを保持する構造体です。

---

### `Enemy::Initialize()` の引数変更

**変更前：**
```cpp
void Initialize(DxLibCommon* dxLib, Vector3 position, Vector3 vel);
```

**変更後：**
```cpp
void Initialize(DxLibCommon* dxLib, Vector3 position, int hp, unsigned int color, float speed);
```

以前は velocity（速度ベクトル）を渡していましたが、今回からは `hp`・`color`・`speed` を個別に渡すように変わりました。  
**なぜ？** → マスターCSVから読んだ値をそのまま渡しやすいため。

---

### `Enemy::TakeDamage()` メソッド（新規追加）

```cpp
// Enemy.cpp
void Enemy::TakeDamage(int damage) {
    hp_ -= damage;
    if (hp_ <= 0) isAlive_ = false;
}
```

**以前は** 弾が当たったら即死（`enemy.Kill()`）でしたが、  
**今回から** HPを減らして0になったら死ぬ仕組みになりました。

```
弾が当たる → TakeDamage(1) → hp_ が 1 減る
                            → hp_ <= 0 なら isAlive_ = false
```

これにより、HP=3の青い敵は**3発撃たないと倒せない**ようになります。

---

### `EnemyManager::LoadFromCSV()` メソッド（新規追加）

最も重要な変更です。CSVを読み込む処理が `main.cpp` から `EnemyManager` に移動しました。

```cpp
void EnemyManager::LoadFromCSV(DxLibCommon* dxLib) {
    // ① マスターデータを読み込んでマップに格納
    std::unordered_map<int, EnemyMasterData> masterMap;
    {
        io::LineReader reader("Resources/enemy_master.csv");
        reader.next_line(); // 1行目（ヘッダ: ID,COLOR,HP,SPEED）をスキップ
        while (char* line = reader.next_line()) {
            // 各列をカンマで分割して読む
            // ...（省略）
            masterMap[data.id] = data;  // IDをキーにして登録
        }
    }

    // ② 配置データを読み込んで敵を生成
    {
        io::LineReader reader("Resources/enemy_placement.csv");
        int row = 0;
        while (char* line = reader.next_line()) {
            // セルの値（ID番号）をチェック
            // ID > 0 かつ masterMap に存在する → 敵を生成
            // ...（省略）
            ++row;
        }
    }
}
```

#### `std::unordered_map` とは？

「キー → 値」の対応表（辞書）です。

```cpp
std::unordered_map<int, EnemyMasterData> masterMap;
// キー: int（敵ID）  値: EnemyMasterData（敵の特性）
```

```
masterMap[1] → { id=1, color=赤, hp=1, speed=5 }
masterMap[2] → { id=2, color=青, hp=3, speed=3 }
masterMap[3] → { id=3, color=黄, hp=5, speed=1 }
```

配置データを読んでいるとき、セルに `2` が書いてあれば `masterMap[2]` を参照して青い敵の情報を取り出せます。

#### 座標計算

配置CSVの行・列番号からワールド座標を計算しています。

```cpp
float x = static_cast<float>(col - kColCenter) * kCellWidth;
// 例: col=6, kColCenter=4, kCellWidth=5.0f
//   → x = (6-4) * 5.0f = 10.0f  （右に10ユニット）

float z = -static_cast<float>(kTotalRows - row) * kCellDepth;
// 例: row=0（最初の行）, kTotalRows=27, kCellDepth=5.0f
//   → z = -(27-0) * 5.0f = -135.0f  （最も遠い位置）
```

---

### `ColorNameToRGB()` 関数（新規追加）

```cpp
static unsigned int ColorNameToRGB(DxLibCommon* dxLib, const char* name) {
    if (strcmp(name, "RED")    == 0) return dxLib->MakeColor(220, 60,  60);
    if (strcmp(name, "BLUE")   == 0) return dxLib->MakeColor(60,  100, 220);
    if (strcmp(name, "YELLOW") == 0) return dxLib->MakeColor(220, 200, 60);
    return dxLib->MakeColor(200, 200, 200); // デフォルト: 白
}
```

CSVに書いた文字列 `"RED"` などを、DxLib が使える色データ（unsigned int）に変換しています。

`strcmp()` は2つの文字列が同じかどうかを比べる関数です（同じなら `0` を返します）。

---

### `main.cpp` の変更

**変更前（約25行）：**
```cpp
EnemyManager enemyManager;
{
    io::LineReader reader("Resources/enemy_data.csv");
    int row = 0;
    while (char* line = reader.next_line()) {
        int col = 0;
        for (char* p = line; *p; ++p) {
            if (*p == 'e' || *p == 'E') {
                float x = (col - kColCenter) * kCellWidth;
                float z = -(kTotalRows - row) * kCellDepth;
                enemyManager.Add(dxLib, { x, 0.0f, z }, { 0.0f, 0.0f, kEnemyVelZ });
            } else if (*p == ',') {
                ++col;
            }
        }
        ++row;
    }
}
```

**変更後（わずか2行）：**
```cpp
EnemyManager enemyManager;
enemyManager.LoadFromCSV(dxLibCommon);
```

`main.cpp` はゲームループの管理に集中し、「敵の読み込み」という責務は `EnemyManager` が担う形になりました。これが**責務の分離（Separation of Concerns）**という設計原則です。

---

## 4. 全体のデータの流れ

```
enemy_master.csv          enemy_placement.csv
┌──────────────┐          ┌──────────────────┐
│ID,COLOR,HP,SP│          │ ,,,,,,,,         │
│1,RED,1,5     │          │ ,1,,,,,,,        │
│2,BLUE,3,3    │          │ ,,,,,,,,2        │
│3,YELLOW,5,1  │          │ ...              │
└──────┬───────┘          └────────┬─────────┘
       │ 読込                       │ 読込
       ▼                           ▼
  masterMap                   セルの数字 → masterMap[id] を参照
  [1]→赤,HP1,速5
  [2]→青,HP3,速3        ──────────────────────────────
  [3]→黄,HP5,速1      → 座標を計算 → Enemy::Initialize() → enemies_ に追加
```

---

## 5. まとめ

| 概念 | 説明 |
|------|------|
| **データドリブンデザイン** | 動作をコードでなくデータ（CSV等）で制御する手法 |
| **マスターデータ** | 敵の種類ごとの特性を定義するデータ |
| **配置データ** | どこに何を置くかを定義するデータ |
| **unordered_map** | キーと値のペアを素早く検索できるコンテナ |
| **責務の分離** | それぞれのクラスが「自分の仕事」だけを担当する設計 |
| **TakeDamage** | HPを段階的に減らすことで複数ヒットが必要な敵を実現 |

---

## 演習

1. `enemy_master.csv` に新しい敵タイプ `4,RED,10,2`（赤・HP10・遅い強敵）を追加し、`enemy_placement.csv` の1か所に `4` を書いて動作を確認しなさい。

2. `ColorNameToRGB()` 関数に `GREEN`（R=60, G=200, B=60）を追加し、CSVで使えるようにしなさい。

3. `EnemyManager::LoadFromCSV()` の中で、敵の総数を `int` 変数でカウントし、読み込み完了後にデバッグ出力（`OutputDebugStringA` や `printf`）で表示しなさい。
